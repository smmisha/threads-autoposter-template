name: Daily Threads Auto-Publisher

on:
  schedule:
    # Runs every day at 09:00 UTC (12:00 Moscow Time)
    - cron: '0 9 * * *'
  workflow_dispatch:
    # Allows manual triggering from the GitHub Actions tab

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Publish Script
        env:
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node publish.js

      - name: Commit and Push Changes (Update thoughts.txt status)
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add thoughts.txt
          # Commit only if there are changes to thoughts.txt
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: update thoughts.txt with publishing status [skip ci]" && git push)
